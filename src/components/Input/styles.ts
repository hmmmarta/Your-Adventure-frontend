import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    input: {
        height: 50,
        marginTop: 40,
        marginHorizontal: 25,
        borderWidth: 1.5,
        paddingLeft: 20,
        borderColor: "#D9D9D9",
        borderRadius: 10,
        color: "#969696",
    },
    textInputFocused: {
        borderColor: "#367AFF",
    },
});

export default styles;